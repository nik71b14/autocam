shift + CTRL + B: debug compile
CTRL + ALT + B: release compile
F5: run debug
Go to folder 'release' and run 'voxelize' by using the command: ./voxelize (check that execution permissions are set with 'chmod +x filename')
